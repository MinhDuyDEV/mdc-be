import { IsNotEmpty, IsString } from "class-validator";

export class EndorseSkillDto {
	// Empty body — skillId and userId come from URL params
}

export class RemoveEndorsementDto {
	// Empty body
}
